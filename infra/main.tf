# Just referencing variables and calling other files
# Terraform automatically loads *.tf files in the directory

terraform {
  required_version = ">= 1.0.0"
}

# Nothing special required here since we're using separate files for resources
